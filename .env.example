# LLM provider keys (dummy placeholders - set real keys in production; can also be
# stored per-workspace via the settings API)
OPENAI_API_KEY=sk-dummy-openai-key
ANTHROPIC_API_KEY=sk-ant-dummy-anthropic-key
GEMINI_API_KEY=dummy-gemini-key
ZHIPUAI_API_KEY=dummy-zhipuai-key
# Port for local dev (uvicorn --port)
PORT=8000
